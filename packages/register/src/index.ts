/**
 * @agentgraph/register — side-effect-only preload entry for
 * `node --import`, `node --require` (via ./cjs), and `bun --preload`.
 *
 * M4 wires the env-configured sdk.init() side effect here (DESIGN §1).
 */

export {};
